replicaCount: 3

image:
  repository: us-docker.pkg.dev/google-samples/containers/gke/hello-app
  tag: "1.0"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

resources:
  requests:
    cpu: 500m
    memory: 512Mi
  limits:
    cpu: 1000m
    memory: 1Gi
